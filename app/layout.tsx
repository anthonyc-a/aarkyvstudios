import Image from "next/image";
import Nav from "./components/nav";
import "./css/globals.scss";
import "./css/webfonts/britti-sans-variable.css";
import type { Metadata } from "next";
import MarqueeCpt from "./components/marquee";

export const metadata: Metadata = {
  title: "Aarkyv Studios",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>
        <Nav />
        <MarqueeCpt/>
        <div className="systems fixed top-10 left-20 underline z-10">systems</div>
        <main>{children}</main>
        <div className="cookies grow w-full max-w-[580px] fixed bottom-4 md:bottom-8 right-0 md:right-8 border border-[#222]">
          <div className="w-full h-full p-6 flex items-center justify-between">
            <p className="flex items-center gap-2 font-[500]">
            <Image src={"/cookie.svg"} alt="" width={17} height={17} />
              This website uses cookies to improve your experience.
            </p>
            <div className="flex gap-6">
              <span className="underline">close</span>
            </div>
          </div>
        </div>
      </body>
    </html>
  );
}
